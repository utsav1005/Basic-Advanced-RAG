# Moved

This doc described the old microservices layout (deleted). Current
architecture: `docs/architecture/overall-system.md`.

Old version kept at `ARCHITECTURE.md.old` for reference during migration
review — delete once you've confirmed nothing else links to it.
